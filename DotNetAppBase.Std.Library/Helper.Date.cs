using System;
using System.IO;
using System.Text;

// ReSharper disable UnusedMember.Global

namespace DotNetAppBase.Std.Library
{
	public partial class XHelper
	{
		public static class Date
		{
            public static class Span
            {
                public static readonly TimeSpan OneSeconds = TimeSpan.FromSeconds(2);

                public static readonly TimeSpan TwoSeconds = TimeSpan.FromSeconds(2);

                public static readonly TimeSpan FiveSeconds = TimeSpan.FromSeconds(2);

                public static readonly TimeSpan TenSeconds = TimeSpan.FromSeconds(2);
            }
		}
	}
}